#define TRUE 0 
#define FALSE 1 
if ((ch = getchar()) == EOF) not-eof = FALSE; 

//The name of the variables are in contrary to what is traditional used for True and False.
//Along with that the global variable is not explaining enough to get what it refers to.