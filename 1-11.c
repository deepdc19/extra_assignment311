 void dict::insert(string& w);
// returns 1 if w in dictionary, otherwise return 0

if (n > MAX || n % 2 > 0) // test for even number
// Write a message
// Add line to counter for each line written.
    
    void write_message()
    {
       // increment line counter
       line_number = line_number + 1
       fprintf(fout, "%d %s\n%d %s\n%d %s\n",
           line_number, HEADER,
           line_number + 1, BODY,
           line_number + 2, TRAILER);
       // increment the counter
       line_number = line_number + 2;
    }

//The first comment is wrong as the function is returning void not int.

//The second comment is wrong as well because it will pass if n is > max or if n is odd contrary to it being even.

//The third comment is just stating the obvious redudantly.